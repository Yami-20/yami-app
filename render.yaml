services:
  - type: web
    name: yami-backend-ge8b
    runtime: docker
    dockerfilePath: ./server-deploy/Dockerfile
    dockerContext: .
    plan: free
    region: oregon
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
